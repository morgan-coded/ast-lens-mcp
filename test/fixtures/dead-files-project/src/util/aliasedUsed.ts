// Imported by index.ts via the tsconfig path alias "@util/aliasedUsed"
// (@util/* -> src/util/*). Resolving the alias to this file is what proves it
// is reachable -> not flagged. A relative-only resolver would wrongly flag it.
export function aliasedUsed(): number {
  return 2;
}
