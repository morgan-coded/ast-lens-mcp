// Target of the explicit-file path alias `@config` -> `src/config/index.ts`.
// Imported by lazy.ts to give the dynamic-import target an onward edge.
export const config = { name: "import-graph-fixture" };
